
import LoginForm from '../components/LoginForm';

const LoginPage = () => {
  return (
    <div>
      <LoginForm />
    <p> <a href="/">Back to Home</a>
    </p>
    </div>
  );
}

export default LoginPage;
